# Сделать рабочее завершение по нажатия клавиши не вышло, поэтому цикл завершается при отсутствии сиимволов в вводе.

total_sum = 0 #переменная для собирания конечного значения суммы всех символов
while True: #цикл
    try:
        char = input("Введите символ: ") #ввод символа
        total_sum += ord(char) #счётчик суммы
    except:
        break #завершение цикла при отсутствии символов
print("Сумма ASCII кодов введенных символов: ", total_sum)
#Коментарий для проверки обновления кода 